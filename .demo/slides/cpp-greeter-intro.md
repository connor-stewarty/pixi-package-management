---
theme: default
layout: default
---

# `cpp-greeter`

A tiny C++ library + CLI that:
- Uses fmt (conda C++ library)
- Builds with CMake
- Has linting via clang-tidy
- Has tests written in pytest that execute the compiled binary
